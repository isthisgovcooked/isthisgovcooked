# Weekly Data Update (DISABLED — enable when agent is ready)
# Uncomment the schedule trigger and implement the agent step when the
# weekly update agent is built. See AGENT.md for roadmap.
#
# on:
#   schedule:
#     - cron: '0 23 * * 0'  # Sunday 9am AEST = Sunday 11pm UTC
name: Weekly Data Update (DISABLED — enable when agent built)

on:
  workflow_dispatch:

jobs:
  update-scores:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run score update
        run: echo "Agent implementation — see AGENT.md"
