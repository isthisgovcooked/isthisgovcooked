#!/usr/bin/env npx tsx
/**
 * Create a GitHub PR with changes to lib/data.ts and/or lib/pm-data.ts.
 * Used by agent-weekly and agent-budget-day workflows after fetch + score-update.
 * Requires GITHUB_TOKEN and repo checkout with changes.
 */
console.error("GovCooked Agent: create-pr (stub) — use peter-evans/create-pull-request in workflow");
process.exit(0);
